import BaseSupplementer from '@src/supplementers/BaseSupplementer';

/**
 * Collects information to supplement a `log` with.
 */
abstract class BaseCollector extends BaseSupplementer {

}

export default BaseCollector;
